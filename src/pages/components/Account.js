import {
  FavoriteRecipesContainer,
  AccountInfo,
} from "../../features/containers";

const Account = () => {
  return (
    <main>
      <AccountInfo />
      <FavoriteRecipesContainer />
    </main>
  );
};

export default Account;
